<div class="container">
  <h2>S'identifier</h2>

<% if params[:param] == "artisan"  %>
    <%= render "artisan" %>
<% else %>
    <%= render "user" %>
<% end %>
  <%= render "devise/shared/links" %>
</div>
